using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TagManager : MonoBehaviour
{
    public enum Tags {
        Player,
    }

    public enum Scenes {
        MainMenu,
        GameScene,
        GameOver
    }
}
